# ===================================
# Filter Scope Definitions
# ===================================

scopes:
    created_at:
        label: kienbt.digitalproduct::lang.plugin.common.created_at
        type: daterange
        conditions: created_at >= ':after' AND created_at <= ':before'

    published:
        label: kienbt.digitalproduct::lang.plugin.common.hide_published
        type: checkbox
        conditions: published <> true